ToDO:


update flux and workflows so we can input loras
Character sheet workflow into subfolder

update parser and launcher so we can modify image prompt and video prompt separately, it will help with story telling


workflow for changing the background of a shot
workflow for generating a 360 hdri of an input

make text wrap in the text editor

make the window start up a bit bigger so we dont have to expand it to see the modes dropdown

* update ct_qwen_cameratransform to output images to subfolders
* refine the ct_qwen_cameratransform angles
* workflow for taking FrontTopLeftRight and making an animated TT
* ct_qwen_cameratransform add mode for FrontBackLeftRight
* update gui to have checkboxes for the available HOSTS to use, make them update depending on the workflow as well.
* fluxgeneration works as it should, creates folders and dosnt fail because of comfyui cache except
* wan generation works.
* Git install works
-------------------------------
